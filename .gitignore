# MSDS 565 — build artifacts and editor scratch. None of this is course material.

# Python
__pycache__/
*.py[cod]

# Jupyter autosave checkpoints
.ipynb_checkpoints/

# LaTeX build output — keep the .tex, not these
*.aux
*.fdb_latexmk
*.fls
*.log
*.nav
*.out
*.snm
*.synctex.gz
*.toc

# Exports of the prompts are generated on demand, not committed.

# Submitted student work never enters this repo; it stays local.
Course Admin/Tracking/Submissions/
